import * as PIXI from "pixi.js";
import { Container, Sprite } from '@pixi/react';
import React from 'react'
import Player from './Player';
import { GameCamera } from "../screen/GameCamera";
import { graphicPath } from "../global/GraphicPaths";
import { useGameManagerStore } from "../game/GameManagerStoreContext";



export default function PlayerRenderer() {

    const ctx = useGameManagerStore();

    const positionX = ctx.player.getPositionX() - ctx.camera.getOffsetX();
    const positionY = ctx.player.getPositionY() - ctx.camera.getOffsetY();
    
    
    // TODO: fix error below
    const outfit_walking = graphicPath.player.walk;



    //TODO: fill
  return (
    <Container  >
        <Sprite anchor={0.5} x={positionX} y={positionY} image={outfit_walking} scale={ctx.player.scale} />
    </Container >
  )
}
